-> Misión 1 <-

Usa awk para mostrar solo la primera columna del archivo:
users.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/users]
└─# awk -F ":" '{print $1}' users.txt
root
kali
mysql



-> Misión 2 <-

Usa awk para mostrar solo la tercera columna del archivo:
users.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/users]
└─# awk -F ":" '{print $3}' users.txt
0
1000
110



-> Misión 3 <-

Usa awk usando  : como delimitador para mostrar solo los shells del archivo:
users.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/users]
└─# awk -F ":" '{print $4}' users.txt
/bin/bash
/bin/zsh
/bin/false



-> Misión 4 <-

Usa awk para mostrar: columna 1 y columna 4 del archivo:
auth.log

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/logs]
└─# awk '{print $1, $4}' auth.log      
May admin
May root
May guest
May admin



-> Misión 5 <-

Usa awk para mostrar solamente las líneas donde la columna 2 sea: Failed en:
ips.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/network]
└─# awk '$2 == "Failed"' ips.txt  
192.168.1.10 Failed
172.16.0.2 Failed



-> Misión 6 <-

Usa awk para mostrar solamente las líneas donde la columna 2 sea mayor que: 200 en:
ventas.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/sales]
└─# awk '$2 > 200' ventas.txt
Juan 300
Pedro 500



-> Misión 7 <-

Usa awk para sumar todos los valores de la columna 2 del archivo:
ventas.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/sales]
└─# awk '{sum+=$2} {print sum}' ventas.txt
300
450
950
1050



-> Misión 8 <-

Usa grep y awk juntos para mostrar solo las IPs de las líneas que contengan: Failed en:
auth.log

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/logs]
└─# grep -i "Failed" auth.log | awk '{print $3}'
192.168.1.10
172.16.0.2
192.168.1.10



-> Misión 9 <-

Usa awk para mostrar:
usuario -> shell

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/users]
└─# awk -F ":" '{print $1 " -> "  $4}' users.txt            
root -> /bin/bash
kali -> /bin/zsh
mysql -> /bin/false




-> Misión 10 <-

Usa awk para contar cuántas líneas tienen: Failed en:
ips.txt

┌──(root㉿kali)-[/home/…/Linux-Labs/awk/awk_lab/network]
└─# awk '$2 == "Failed" {count++} END {print count}' ips.txt 
2




